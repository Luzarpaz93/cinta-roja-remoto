const NumerosEnteros = [ 1 , 1 , 2 , 3 , 4 , 5];

const NumeroRepetido = [1];
for(let i = 0; i<NumerosEnteros.lenght; i ++) {
      const numero = NumerosEnteros[i];
      if(obj = 1) {
             NumeroRepetido.push(obj)
}
}

console.log(NumeroRepetido);